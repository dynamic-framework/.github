name: PR Labeler

on:
  workflow_call:

jobs:
  pr-labeler:
    permissions:
        contents: read
        pull-requests: write
        issues: write
    runs-on: ubuntu-latest
    steps:
      - name: Labeler
        uses: actions/labeler@v6
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}